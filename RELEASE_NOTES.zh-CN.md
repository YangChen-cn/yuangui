# 元圭与 VCC 更新说明

[English](RELEASE_NOTES.md)

## 2.7.2 — 自动更新、Gitee 镜像与发布稳定性

- 新增安静的自动更新流程：启动延迟、每日补查次数限制、待展示更新状态和后台不抢焦点均已明确处理。无网络或存在系统模态界面时保持静默。
- 用严格居中的 SwiftUI 更新窗口替换不稳定的更新提示，支持“稍后”“查看更新内容”和“立即更新”，并继续复用现有更新 Store 与安装流程。
- 加入 Gitee 国内更新镜像，同时保持 GitHub 为权威主源。应用会先请求 GitHub；只有主源暂时没有结果时才延迟启动 Gitee 备用请求，合法的 GitHub 清单始终优先。只有明确的网络可用性错误允许回退，清单格式或安全校验错误不会被静默切换到另一来源掩盖。
- 下载流程也遵循同一主备策略：GitHub DMG 校验成功后直接使用；只有 GitHub 无法建立或维持网络连接时才尝试 Gitee。校验和、文件大小、Bundle、版本、挂载或代码签名失败会立即停止，不会被误判为镜像网络故障。
- 修复延迟回退截止时间：GitHub 主源超时后，已经启动的 Gitee 请求仍可完成；如果本次检查使用 Gitee 清单，安装时会复用已确认的 Gitee 来源，不再重复等待 GitHub。
- 强化更新安全校验：要求 HTTPS，严格验证清单字段，流式计算 SHA-256，检查文件大小、DMG 挂载、Bundle ID、版本、Build、最低系统版本和代码签名后才允许安装。
- 恢复完整的手动检查更新详情和中英文 Release Notes 选择，手动检查不再被自动更新提示的“两条摘要”限制覆盖。
- 增加发布自动化：将同一个 DMG 和更新清单镜像到 Gitee，稳定通道拒绝预发布版本，从 DMG 读取真实版本信息，串行发布清单，并阻止意外的版本降级覆盖。
- 修复 Dashboard 工具栏点击“检查更新”后状态栏页面被关闭的问题：检查会等弹出层完成展示后再启动，外部点击监视器也会忽略辅助弹出窗口。
- DMG 打包脚本现在必须显式传入稳定版 `VERSION` 和正整数 `BUILD`，避免使用过期默认值生成错误版本的安装包。
- 在不降低关键行为覆盖率的前提下精简 CI：重复的更新与生命周期场景改为表驱动测试，翻译和日记 benchmark 移到独立测试目标，不再拖慢普通单元测试。

## 更早版本

- 新增本地音乐、更安全的清理流程，改进歌词与番茄钟工具，并补齐双语界面。（2.7.0）
- 优化音乐性能和 SwiftUI 生命周期隔离，加入桌宠专注动作、迷你播放器状态反馈、歌词气泡尺寸调整和 Liquid Glass 展示。（2.7.1）
- 更早版本还加入日记备份恢复、贴边收纳、紧凑监控，以及 macOS 26 Liquid Glass 和旧系统 Material fallback。（2.6.0–2.6.1）
