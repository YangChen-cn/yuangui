# 元圭与 VCC 更新说明

[English](RELEASE_NOTES.md)

## 2.8.1 — Finder 右键与更新可靠性改进

- 调整 FinderSync 菜单顺序：新建文件、剪切、粘贴、复制、在终端中打开、在编辑器中打开。菜单不再使用装饰标题行，也不会插入多余留白。
- 新增“新建空白文件”，使用轻量的原生名称输入窗口。可以直接输入 `config.json`、`main.swift` 等完整文件名；无效路径会被拒绝，遇到重名时会保留扩展名并自动选择新名称。
- “复制”菜单新增复制文件名、完整路径和终端参数，支持多选；终端参数会按 shell 规则转义。
- “在终端中打开”会打开选中的文件夹；选中文件时打开其所在目录。一级菜单直接使用最近一次仍可用的终端，单独的选择菜单列出本机终端并勾选当前默认项。
- 编辑器采用相同逻辑：选中的文件或文件夹会直接交给最近使用且仍已安装的编辑器；本机有多个受支持编辑器时可从选择菜单切换。
- Kaku 改用自身注册的“New Kaku Tab Here” macOS Service 打开所选目录；Service 不可用时才回退为普通启动。
- 修复空白文件输入框无法获得键盘焦点的问题，并改进剪切、粘贴的目标判断和重名处理。
- 开发重建和应用更新只刷新一份 Finder 扩展注册，同时保留用户原有的启用状态，避免重复菜单或菜单短暂出现后消失。
- 修复慢速下载的测速窗口，始终按最近 10 秒计算。GitHub 长时间限速时不会因为累计历史变长而被误判为恢复，可继续切换到同一清单中的 Gitee 资源。
- 收紧下载完成与取消处理。测速和停滞监控不会在文件写入即将结束时抢先中止任务，用户取消也不会被当成下载成功。
- 检查更新和安装统一使用版本号与 build 判断：版本号相同时只接受更高 build；切换固定更新源后，旧更新源发起的结果会被丢弃并重新检查。
- 「关于」页面新增下载进度显示：实时显示当前下载源（GitHub 或 Gitee）、百分比和已下载字节数，并带线性进度条；自动切换来源后进度会按新来源重新计算，而不是沿用旧百分比。
- 下载过程不再受进度界面影响：传输通过独立通道上报进度，界面繁忙时不会把下载拖慢到触发来源切换。
- 网络传输一结束就进入“准备安装”阶段，校验和、签名和挂载检查不再显示为卡在 100% 的下载。
- 自动更新弹窗点击“立即更新”后会先打开「关于」页面，下载进度在安装期间始终可见。
- 改进 Gitee Release 资源处理：同名资源最终只保留一份经过字节校验的副本；校验下载失败时不会删除状态不明的文件，上传完成后也会重新读取确认。
- 发布脚本会在产生外部改动前确认 `main` 工作区干净且提交已推送，并通过提交、标签、build 和运行编号锁定本次镜像任务，避免误跟踪其他工作流。

## 更早版本

- 新增本地音乐、更安全的清理流程，改进歌词与番茄钟工具，并补齐双语界面。（2.7.0）
- 优化音乐性能和 SwiftUI 生命周期隔离，加入桌宠专注动作、迷你播放器状态反馈、歌词气泡尺寸调整和 Liquid Glass 展示。（2.7.1）
- 加入安静的自动更新、以 GitHub 为权威主源的 Gitee 可用性镜像、更严格的更新校验、更安全的打包流程和发布自动化。（2.7.2）
- 加入原生翻译朗读、更可靠的音乐窗口、桌宠新手导览与右键工具、首版 FinderSync 菜单，以及可选更新源和慢速下载切换。（2.8.0）
- 更早版本还加入日记备份恢复、贴边收纳、紧凑监控，以及 macOS 26 Liquid Glass 和旧系统 Material fallback。（2.6.0–2.6.1）
