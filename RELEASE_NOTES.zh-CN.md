# 元圭与 VCC 更新说明

[English](RELEASE_NOTES.md)

## 2.8.2 — 稳定维护版

- Finder 新建文档会直接弹出原生命名框，并预选完整建议文件名；下一步直接输入名称即可，扩展名也能一起修改。
- 菜单栏音乐面板会为最近显示过的本地和在线封面保留受限内存缓存；重新打开时不再闪现占位封面，同时不会让隐藏面板继续进行 SwiftUI 刷新。
- FinderSync 启动时复用已缓存的终端与编辑器列表，随后仅在应用启动或退出时刷新，避免扩展重启后每次右键都重新枚举应用而延迟显示。
- 完整播放器的哔哩哔哩和本地音乐行现在整行都可作为双击播放目标，空白区域也能稳定切歌。
- 哔哩哔哩歌曲已匹配的 LRCLIB 歌词会被保留；后续出现或登录后刷新的哔哩哔哩字幕只作回退，不再在播放重载时覆盖它。
- 菜单栏音乐面板复用桌宠小窗的可点击封面：点击封面会关闭面板并打开完整播放器。
- 开发构建和 DMG 打包默认使用已注册的 `YuanGui` 自签名身份，不再使用临时签名；本机找不到该身份时脚本会明确失败。

## 更早版本

- 新增本地音乐、更安全的清理流程，改进歌词与番茄钟工具，并补齐双语界面。（2.7.0）
- 优化音乐性能和 SwiftUI 生命周期隔离，加入桌宠专注动作、迷你播放器状态反馈、歌词气泡尺寸调整和 Liquid Glass 展示。（2.7.1）
- 加入安静的自动更新、以 GitHub 为权威主源的 Gitee 可用性镜像、更严格的更新校验、更安全的打包流程和发布自动化。（2.7.2）
- 加入原生翻译朗读、更可靠的音乐窗口、桌宠新手导览与右键工具、首版 FinderSync 菜单，以及可选更新源和慢速下载切换。（2.8.0）
- Finder 右键与更新可靠性改进（2.8.1）
- 更早版本还加入日记备份恢复、贴边收纳、紧凑监控，以及 macOS 26 Liquid Glass 和旧系统 Material fallback。（2.6.0–2.6.1）
