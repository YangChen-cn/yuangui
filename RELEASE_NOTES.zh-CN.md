# 元圭与 VCC 更新说明

[English](RELEASE_NOTES.md)

## 2.9.0 — PDF 转 Markdown

- 截图选区松开鼠标后仍可调整，支持八个手柄、键盘微调、正方形与中心缩放，以及紧凑快捷工具条。区域、窗口和屏幕截图共用复制、保存、编辑、识字、翻译和固定操作，快捷操作面板不再强制每张截图进入编辑器。
- 新增独立快捷键的“截图识字”，识别后直接复制；支持截图固定、延迟框选和恢复上次选区。编辑器增加标注选择、移动、删除、样式修改、原位文字、数字标记、Core Image 模糊，以及打开、拖入和粘贴已有图片。
- 入口扩展为“文档转 Markdown”：PDF/XPS/EPUB/FB2 复用固定运行时，TXT 和 PNG/JPEG/TIFF 走原生文本/Vision 路径，无需 Python；按格式隐藏不相关的 PDF 选项。
- 修复卸载时锁文件身份被破坏的问题，修正真实安装测试的裁剪路径验证；本轮不改变运行时版本和裁剪范围。

- 新增在 Apple Silicon 上运行的本地 PDF 转 Markdown 窗口（PyMuPDF Layout 1.28.2）：选择或拖入一个 PDF，预览源码、复制正文，并导出 Markdown 与配套 `_assets` 文件夹。插图（含矢量图）保留在正文对应位置，不再统一堆到文末。
- 默认移除每页重复的页眉页脚（文档标题、页码、版权行），可用开关保留。
- 选择 PDF 后先做一次亚秒级预检：页数、是否加密、是否存在文字层。加密文档在加载版面模型前直接失败；疑似扫描件会提示开启 OCR。
- 混合式 OCR 默认开启，仅对需要的页面调用中英文识别：自带文字层的 PDF 转换结果完全一致，也不会加载识别组件。开关状态会被记住，且不会用 OCR 强制覆盖页面已有的文字层。
- 首次使用安装独立的 Apple Silicon 专用 Python 环境和经过校验的转换依赖。安装结束时会删除本配置不会读取的文件——未使用的版面/表格模型、未使用的依赖以及安装器自身的工具，全新安装体积从约 490 MB 降到实测 358 MB。
- PDF 窗口显示转换组件占用的磁盘空间，并支持一键卸载；校验通过的新版本仍会替换旧运行时目录。
- 大文档预览限制为前 200,000 字符，导出保留全文。导出重名时自动编号，失败时清理本次未完成产物，保留已有文件。
- 缩小更新进度变化时工具面板的刷新范围；手帐 Markdown 仅在正文变化时重新解析。
- 复杂阅读顺序、表格和代码仍需检查；不重建公式，OCR 准确度取决于扫描质量。

## 2.8.2 — 稳定维护版

- Finder 新建文档会直接弹出原生命名框，并预选完整建议文件名；下一步直接输入名称即可，扩展名也能一起修改。
- 菜单栏音乐面板会为最近显示过的本地和在线封面保留受限内存缓存；重新打开时不再闪现占位封面，同时不会让隐藏面板继续进行 SwiftUI 刷新。
- FinderSync 启动时复用已缓存的终端与编辑器列表，随后仅在应用启动或退出时刷新，避免扩展重启后每次右键都重新枚举应用而延迟显示。
- 完整播放器的哔哩哔哩和本地音乐行现在整行都可作为双击播放目标，空白区域也能稳定切歌。
- 哔哩哔哩歌曲已匹配的 LRCLIB 歌词会被保留；后续出现或登录后刷新的哔哩哔哩字幕只作回退，不再在播放重载时覆盖它。
- 菜单栏音乐面板复用桌宠小窗的可点击封面：点击封面会关闭面板并打开完整播放器。
- 开发构建和 DMG 打包默认使用已注册的 `YuanGui` 自签名身份，不再使用临时签名；本机找不到该身份时脚本会明确失败。

## 更早版本

- 新增本地音乐、更安全的清理流程，改进歌词与番茄钟工具，并补齐双语界面。（2.7.0）
- 优化音乐性能和 SwiftUI 生命周期隔离，加入桌宠专注动作、迷你播放器状态反馈、歌词气泡尺寸调整和 Liquid Glass 展示。（2.7.1）
- 加入安静的自动更新、以 GitHub 为权威主源的 Gitee 可用性镜像、更严格的更新校验、更安全的打包流程和发布自动化。（2.7.2）
- 加入原生翻译朗读、更可靠的音乐窗口、桌宠新手导览与右键工具、首版 FinderSync 菜单，以及可选更新源和慢速下载切换。（2.8.0）
- Finder 右键与更新可靠性改进（2.8.1）
- 更早版本还加入日记备份恢复、贴边收纳、紧凑监控，以及 macOS 26 Liquid Glass 和旧系统 Material fallback。（2.6.0–2.6.1）
