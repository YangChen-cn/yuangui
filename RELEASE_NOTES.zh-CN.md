# 元圭与 VCC 更新说明

[English](RELEASE_NOTES.md)

## 2.7.2 — 自动更新、Gitee 镜像与发布稳定性

- 新增安静的自动更新流程：启动延迟、每日补查次数限制、待展示更新状态和后台不抢焦点均已明确处理。无网络或存在系统模态界面时保持静默。
- 用严格居中的 SwiftUI 更新窗口替换不稳定的更新提示，支持“稍后”“查看更新内容”和“立即更新”，并继续复用现有更新 Store 与安装流程。
- 加入 Gitee 国内更新镜像，与 GitHub 并行获取 `updates/latest.json`。应用会比较所有有效清单，选择更新版本最高且兼容的结果；下载时 Gitee 失败后按类型回退 GitHub。
- 强化更新安全校验：要求 HTTPS，严格验证清单字段，流式计算 SHA-256，检查文件大小、DMG 挂载、Bundle ID、版本、Build、最低系统版本和代码签名后才允许安装。
- 恢复完整的手动检查更新详情和中英文 Release Notes 选择，手动检查不再被自动更新提示的“两条摘要”限制覆盖。
- 增加发布自动化：将同一个 DMG 和更新清单镜像到 Gitee，稳定通道拒绝预发布版本，从 DMG 读取真实版本信息，串行发布清单，并阻止意外的版本降级覆盖。
- 修复 Dashboard 工具栏点击“检查更新”后状态栏页面被关闭的问题：检查会等弹出层完成展示后再启动，外部点击监视器也会忽略辅助弹出窗口。
- DMG 打包脚本现在必须显式传入稳定版 `VERSION` 和正整数 `BUILD`，避免使用过期默认值生成错误版本的安装包。

## 2.7.1 — 音乐性能、SwiftUI 生命周期与桌宠体验优化

- 音乐界面改为精细订阅。播放进度、控制按钮、歌词、资料库、账号、哔哩哔哩导入和本地导入分别观察自己的 Store，不再由一个宽订阅包装器带动整片界面刷新。
- 音乐实现拆分为播放、资料库、歌词、哔哩哔哩、本地文件和持久化 Coordinator；现有资料库格式、服务接口和用户操作保持兼容，不需要迁移数据。
- 补齐取消与退出保护。扫码轮询、收藏夹导入、本地导入、文件重定位、封面维护、歌词搜索、Apple Music 刷新和 URL 播放器回调在取消或 shutdown 后都不会提交过期结果。
- 修复单次哔哩哔哩收藏夹导入可能出现重复歌曲、取消后仍增加进度、歌词搜索一直显示加载中，以及导入或重定位中断后遗留封面文件的问题。
- 未登录哔哩哔哩时不再发起账号刷新请求；清理屋只扫描已启用的类别，去掉了曾让 GitHub Actions 测试阶段超过 9 分钟的网络等待和无关文件系统遍历。
- 新增 Store 发布隔离和挂起服务生命周期测试。实测连续发布 100 次导入进度时，`MusicPlayerView` 的计算次数由 103 次降至 2 次，`MusicProgressView` 由 103 次降至 1 次。
- 收窄 Dashboard、桌宠、设置、日记和音乐界面的 SwiftUI 刷新边界；面板宿主保持稳定，Dashboard 隐藏后也不会继续响应高频状态更新。
- 为三种角色新增专注动作图。开始专注后自动收起侧边控制；锁定时隐藏音乐和锁定控制，只保留只读倒计时圆标，专注结束后退出专注动作。
- 迷你播放器的播放、歌词和锁定状态现在有明确的蓝色选中反馈；从迷你播放器打开完整播放器时会先关闭气泡，并可靠地激活完整播放器窗口。
- 歌词气泡根据实际歌词和提醒文字调整宽度，紧急提醒出现、消失、脉冲和提醒模式切换时都会同步重算，避免提醒被裁切或窗口残留过宽。
- 优化 Dashboard 的 Liquid Glass 选中效果，让同一个选中指示器在页签之间自然移动；内容层保持克制，并继续为 macOS 15–25 保留 Material fallback。

## 2.7.0 — 本地音乐、清理屋与桌宠体验全面升级

- **全新的本地音乐能力**：在 Apple Music 和哔哩哔哩之外加入完整的“本地音乐”来源。可通过系统面板导入 MP3、M4A、AAC、WAV 和 AIFF 文件或文件夹，并支持安全作用域书签、元数据与内嵌封面读取、本地歌单、收藏、播放进度恢复及文件丢失后的重新定位。
- **更完整的音乐资料库**：本地音乐与哔哩哔哩均支持搜索和稳定的展示排序；新增导入失败详情、在 Finder 中显示，以及删除歌曲、重复导入和恢复资料库后的无主封面缓存清理。
- **封面可自由导入**：本地歌曲可以选择或移除自定义封面。重新定位文件时会刷新标题、歌手、专辑、时长、封面和同名 LRC，同时保留歌曲 ID、收藏状态与歌单引用。
- **清理屋安全性和交互重做**：扫描器与执行服务只在用户主动操作后创建，应用启动不再触碰桌面等受保护目录。新增项目扫描位置、分类摘要、硬链接去重、风险分级快捷操作、执行前身份复核、共享数据保护、运行中应用提示和更清晰的选择流程；项目构建产物与旧安装包只会移入废纸篓。完整清理屋的扫描范围、操作记录和确认操作也已补齐中英文本地化。
- **播放器与歌词改进**：本地文件和网络音频共用更一致的播放状态；同目录同名 LRC 优先于歌词缓存和 LRCLIB，并继续支持歌词点击跳转、偏移调整、桌面歌词、外部声音自动暂停及桌宠听歌动作。
- **番茄钟与 AI 聊天体验改进**：番茄钟浮层去掉过大的圆环和重复时长，状态与完成反馈完整适配“减弱动态效果”；AI 流式回复只在聊天显示时以最多 50ms 一次的频率发布，隐藏期间仅写入非 UI 缓冲。可取消的聊天展示状态机现在会先完成卡片退出，再原子恢复紧凑桌宠窗口。
- **桌宠对白升级**：元圭、VCC 和一起模式新增早晨、下午、晚上与深夜对白，并按“语言 + 角色”避免最近五条重复。
- **稳定性修复**：修复 AI 返回格式兼容、默认提示词语言跟随、过期聊天关闭任务、跨会话流式内容串入、短桌宠提示条过宽、桌宠歌词气泡截断、空日记特殊页面无法从顶部新建、封面缓存生命周期、英文清理屋残留中文，以及正式应用错误回退到 SwiftPM 构建资源而触发桌面目录权限的问题。
- **新增英文界面**：支持跟随系统、英文和简体中文切换；英文默认提示词、应用元数据、安装权限指南和界面资源均已补齐，并附完整 GPL-3.0-only、素材许可与第三方声明。

## 2.6.1 — 稳定性与性能优化

- 修复桌面歌词重启恢复与番茄钟时长调整稳定性。
- 延迟创建较重窗口和后台任务，优化常驻内存。

## 2.6.0 — 贴边收纳与 Liquid Glass

- 新增更可靠的日记保存、备份和恢复。
- 新增贴边收纳、紧凑监控，以及 macOS 26 Liquid Glass 与旧系统 Material 降级。
