# 元圭与 VCC 更新说明

[English](RELEASE_NOTES.md)

## 2.8.0 — 原生翻译朗读与更可靠的音乐窗口

- 重新设计划词翻译窗口，采用更轻、更紧凑且与 YuanGUI 统一的 macOS 界面。原文、译文、状态、语言选择和文字替换操作拥有更清晰的层级，动态窗口尺寸也更加稳定。
- 为原文和译文加入 macOS 原生语音朗读，提供相互独立的控制和按语言选择系统声音的能力。同一时间只朗读一侧；原文、目标语言、翻译结果或窗口生命周期变化时会立即停止过时内容。
- 改进翻译与桌宠之间的状态反馈，同时确保翻译活动不会覆盖低电量、内存紧张等紧急提醒。
- 改进 Dashboard 的 Apple Music 展示并强化播放同步。轮询意外中断后能够自行恢复，切歌不再长期停在上一首末尾，桌宠解除贴边时也会立即刷新播放状态。
- 提高 LRCLIB 匹配兼容性：允许缺少时长的数据；“歌名与歌手”匹配失败后会再按歌名搜索，使未填写歌手的歌词条目仍能被找到。
- 新增歌词中文显示转换，可按系统语言自动选择简体或繁体，也可手动指定或关闭转换；转换仅作用于展示层，不改变原始 LRC、搜索结果和时间轴。
- 恢复紧凑的 SwiftUI 迷你播放器 popover，并按照真实 AppKit popover 生命周期重做完整播放器交接。
- 迷你播放器现在可点击歌曲封面打开完整播放器，并扩大播放/暂停圆形按钮的有效点按区域。
- 修复首次打开迷你播放器时生命周期竞态问题
- 为朗读失效、翻译窗口尺寸、紧急提醒优先级、Apple Music 同步恢复、LRCLIB 回退和迷你播放器窗口交接增加针对性测试。
- 新增原生 FinderSync 右键扩展，支持创建常用文本和 Office 文件、复制路径、在已安装的终端应用（包括 Kaku）中打开选中的文件夹，以及在 Finder 中剪切和粘贴项目。
- Finder 扩展操作已适配 Finder 的 XPC 菜单传递机制，并采用不会覆盖已有文件的唯一命名；开发重建和应用更新会原路径刷新扩展，不切换用户开关，也不会重复注册副本。
- 首次启动时由元圭和 VCC 亲自引导：桌宠气泡介绍自己，带你认识右键工具菜单（桌宠锁定时会提供“临时解锁”），并直接试一次真实的截图和截图翻译，最后介绍藏在 Finder 里的右键增强。每一步都可以跳过，完成后记录版本，之后不再自动出现。
- 桌宠右键菜单成为工具入口：区域截图、截图翻译、划词翻译、专注、音乐、手帐、Finder 右键增强、新手导览和设置。菜
- 新增一次性功能发现提示（划词翻译、截图翻译、专注、手帐、音乐、Finder 增强）：只在桌宠空闲时出现，自动跳过你已经用过的功能，并可在设置 → 通用中关闭。老用户升级不会被误判为新安装。
- 引导气泡遵循与桌宠消息一致的优先级：紧急提醒和进行中的任务优先于引导，引导优先于歌词、天气和日常闲聊；气泡高度按内容自适应，不再截断长文本。
- 引导中的“临时解锁”只是运行时状态：不会修改持久化的交互锁定偏好，并在每次退出引导时恢复。
- 截图步骤不会再卡住引导：截图会话同步返回是否真正启动（true/false），每个已启动的会话恰好报告一次结束（成功、取消或失败）。
- GitHub 优先的下载现在会监测持续传输速度：若 10 秒窗口内平均速率低于 50KB/s，应用会切换到同一清单中的 Gitee 资产，并记住该决定 30 分钟，能连但被限速的网络不再卡住更新。
- 设置 → 关于 中可以固定更新源：自动（默认，GitHub 权威并带慢速切换）或强制使用 GitHub / Gitee，检查和下载都会遵循该选择。

## 更早版本

- 新增本地音乐、更安全的清理流程，改进歌词与番茄钟工具，并补齐双语界面。（2.7.0）
- 优化音乐性能和 SwiftUI 生命周期隔离，加入桌宠专注动作、迷你播放器状态反馈、歌词气泡尺寸调整和 Liquid Glass 展示。（2.7.1）
- 加入安静的自动更新、以 GitHub 为权威主源的 Gitee 可用性镜像、更严格的更新校验、更安全的打包流程和发布自动化。（2.7.2）
- 更早版本还加入日记备份恢复、贴边收纳、紧凑监控，以及 macOS 26 Liquid Glass 和旧系统 Material fallback。（2.6.0–2.6.1）
